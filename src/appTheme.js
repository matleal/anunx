import { createTheme } from "@mui/material/styles";

const appTheme = createTheme({
    pallete: {
        primary: {
            main: '#000000',
        },
        background: {
            white: 'red',
        }
    },
    spacing: 8,
});

export default appTheme;